{
  "name": "danbron-railway-service",
  "private": true,
  "scripts": {
    "build": "npm --prefix backend ci",
    "start": "npm --prefix backend start"
  },
  "engines": {
    "node": ">=20"
  }
}
